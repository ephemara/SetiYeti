# BEAST science stack — wire every tool. Core ships with scoop python;
# extras install as binary wheels (no compiler needed).
#   C:/scoop/apps/python/current/python.exe -m pip install -r requirements-science.txt
numpy>=1.26
scipy>=1.12
astropy>=6
h5py>=3.8
matplotlib>=3.8
tqdm>=4.66
PyYAML>=6
pandas>=2.0
scikit-learn>=1.3
pytest>=8.0
sgp4>=2.2       # satellite TLE propagation (satpass.py; pure python, tiny)
# optional accelerators (graceful fallback if absent):
# numba>=0.59      # JIT for fold/DM hot loops (heavy: llvmlite)
# pyfftw>=0.13     # FFTW bindings for SCD/stft paths
# zarr>=2.16       # chunked array store for chained stares
# torch>=2.2       # only if the learned-trajectory path is revived
