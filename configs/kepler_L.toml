# configs/kepler_L.toml — L-band preset (TRAPPIST/Kepler geometry family).
# Header TBIN still wins for fs; these are the search knobs with reasons.
fam_trig = 3.0      # smoke noise 2.2–2.8; clears it, keeps 358 Hz family
spec_line = 5.0
spec_hump = 2.5
sparkle_max = 25
seg = 32768
topk = 15
comb_min_members = 3
comb_min_score = 6.0
frame_sigma = 40.0
fold_fmin = 1.0
fold_fmax = 2000.0
fold_sigma = 16.0
pulse_sigma = 14.0
persist_min = 4
multichan_min = 3
workers = 4
pol_list = [0, 1, 2, 3]
blocks = "0-127"
line_exclude = [0]
